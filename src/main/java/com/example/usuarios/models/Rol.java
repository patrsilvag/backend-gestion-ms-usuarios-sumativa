package com.example.usuarios.models; 

/**
 * Enum para definir los roles permitidos en el sistema.
 * Esto evita errores de escritura y asegura la integridad en Oracle Cloud.
 */
public enum Rol {
    ADMIN,
    CLIENTE
}